{
  "name": "nft-real-estate",
  "version": "0.1.0",
  "description": "Minting Real Estate NFS",
  "author": "hoangnh",
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.6",
    "@nomiclabs/hardhat-etherscan": "^3.0.3",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "@openzeppelin/contracts": "^4.6.0",
    "@truffle/hdwallet-provider": "^2.0.8",
    "chai": "^4.3.6",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.6.6",
    "hardhat": "^2.9.5",
    "truffle": "^5.5.14"
  },
  "scripts": {
    "start-fe": "cd src/ido-frontend && yarn start",
    "start-be": "cd src/ido-backend && yarn start",
    "compile": "truffle compile",
    "deploy": "yarn compile && hardhat run --network rinkeby ./scripts/deploy.js",
    "deploy-ganache": "yarn compile && hardhat run --network ganache ./scripts/deploy.js",
    "verify": "node ./scripts/verify.js"
  }
}
